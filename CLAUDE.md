# SwiftOpenSkills

## Architecture

SwiftOpenSkills is a Swift package providing <!-- describe purpose here once specs are defined -->.

### File Structure

- `Sources/SwiftOpenSkills/SwiftOpenSkills.swift` — Core types and capabilities (to be built out per spec)

### Design Patterns

<!-- Document design patterns as they emerge during spec-driven development. -->

### Dependencies

None yet — dependencies will be added as specs define requirements.

### Build & Test

```bash
swift build
swift test
```

## Spec-Driven Development

This project follows a spec-first workflow. See `docs/SpecDrivenDevelopment.md` for the process and `Spec/SwiftOpenSkills.md` for the current specification.

Skills for AI coding agents will be added to `skills/` as features are defined.
